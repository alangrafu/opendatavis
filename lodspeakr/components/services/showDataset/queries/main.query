PREFIX ex: <{{lodspk.ns.local}}>
SELECT ?row ?col ?val WHERE{
  GRAPH <{{lodspk.args.all|deurifier}}>{
    ?x ex:hasRecord ?y.
    ?y ex:rowNumber ?row;
       ex:hasCell ?cell.
    ?cell ex:value ?val;
          ex:colNumber ?col . 
    FILTER(?row < 7)
  }
}ORDER BY ASC(?row) ASC(?col)
