{%include "../../includes/header.inc"%}
  <div class="container-fluid">
   <h1>Datasets available</h1>
  {%if first.main.g%}
    {% for row in models.main %}
   <div class="row">
    <div class="span12">
     <h2>{{row.url.value|explode:"/"|pop}}</h2>
     <p>Taken from {{row.url.value}} on {{row.dateDownloaded.value|date:"M-y-d \\a\\t G:i:s"}} </p>
     <div class="btn-group">
      <a href='{{lodspk.home}}showDataset/{{row.g.mirroredUri|urifier}}'>
       <button class="btn">Create visualization</button>
      </a>
      <a href='{{row.g.value|deurifier}}'>
       <button class="btn"><i class="icon-download-alt"></i> Download Dataset</button>
      </a>
     </div>
   </div>
</div>
    {% endfor %}      
  {%else%}
    <div class="alert alert-info">
      <p>No named graphs found</p>
    </div>
  {%endif%}
    </div>
  </body>
</html>
