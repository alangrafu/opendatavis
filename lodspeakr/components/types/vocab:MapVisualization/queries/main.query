PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?creationdate ?dataset WHERE{
  GRAPH <{{uri}}>{
    <{{uri}}> dcterms:created ?creationdate;
              prov:wasDerivedFrom ?dataset .
  }
}
