{
  "visualization": {
      "chartType": "{{first.main.vizType.value|explode:"/"|pop}}",
      "dataset": "{{first.main.dataset.value}}",
      "params": {
            "lon": "{{first.params.paramX.value}}",
            "lat": "{{first.params.paramY.value}}"
      },
      "filter": [ 
                {
                  "searchString": "{{first.params.filterValue.value}}",
                  "searchField": "{{first.params.filterColumn.value}}"
                }
              ],
      "sortcol": "",
      "manualdata": true,
      "width": 600,
      "height": 400,
      "readonly": false
  }
}
